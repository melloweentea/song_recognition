# Song Recognition Webapp
To run, git clone this repo and run python -m streamlit run app.py in the terminal. Unfortunately, I can't deploy this to streamlit cloud because it goes over resource limits.
